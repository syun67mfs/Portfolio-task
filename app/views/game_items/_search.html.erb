<h4 class="text-center">プラットフォーム</h4>
<table class="table table-hover table-inverse">
  <% @genres.each do |genre| %>
    <tr>
      <td class="text-center">
        <%= link_to "#{genre.name}", game_items_path(search: genre.id) %>
      </td>
    </tr>
  <% end %>
</table>